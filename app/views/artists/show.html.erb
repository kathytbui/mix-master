<section>
  <article class="">
    <h1><%= @artist.name %></h1>
    <p><%= image_tag @artist.image_path, alt: @artist.name %></p>
    <%=  link_to "Edit", "/artists/#{@artist.id}/edit"%>
    <%=  link_to "Delete", "/artists/#{@artist.id}/edit", method: :delete %>
  </article>
</section>
